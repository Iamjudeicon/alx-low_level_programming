#include <stdio.h>

/**
  * main - print the string
  * Return: an int value 0 else non-zero
  */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
